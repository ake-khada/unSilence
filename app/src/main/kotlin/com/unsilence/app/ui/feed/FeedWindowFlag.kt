package com.unsilence.app.ui.feed

object FeedWindowFlag {
    // FLIP TO true IN DEV BUILDS ONLY TO TEST THE NEW LOADER.
    // Main-branch default must stay false until Phase 2 validation completes.
    const val USE_WINDOW_LOADER = false
}
